{
  "type": "object",
  "additionalProperties": {
    "type": "object",
    "required": ["daac", "startDate", "endDate", "variables"],
    "properties": {
      "daac": { "type": "string" },
      "startDate": { "type": "string", "format": "date" },
      "endDate": { "type": "string", "format": "date" },
      "variables": { "type": "array", "items": { "type": "string" } }
    }
  }
}